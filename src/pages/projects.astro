---
import { BaseLayout } from "../layouts";
import { Spacer } from "../components";
import { projects, categories } from "../data";
---

<BaseLayout pageTitle="Projects">
  <h2 class="category-heading">Web</h2>
  {projects.map((project) => project.category === categories.web && <h3 id={project.title.toLowerCase().replace(/ /g, '-')}>{project.title}</h3>)}

  <h2 class="category-heading">Mobile</h2>
  {projects.map((project) => project.category === categories.mobile && <h3>{project.title}</h3>)}
  
  <h2 class="category-heading">Artwork</h2>
  {projects.map((project) => project.category === categories.artwork && <h3>{project.title}</h3>)}

  <p class="temp"><i>Don't forget to add tags for tech used like the home page and link to sites/repos.<br>Also probably show a tag to FamilySearch for projects completed there.</i></p>

  <Spacer size="xxl" />
  <Spacer size="xxl" />
</BaseLayout>

<style>
  .temp {
    background: var(--foreground-transparent);
    padding: 16px;
    border-radius: var(--border-rad);
    text-align: center;
    color: var(--text-secondary)
  }

  .category-heading {
    font-size: 36px;
    font-weight: 600;
  }
</style>
